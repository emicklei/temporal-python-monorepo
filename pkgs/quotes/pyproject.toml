[project]
name = "quotes"
version = "0.1.0"
description = "Shared quote utilities"
authors = [{ name = "monorepo" }]
dependencies = []
requires-python = ">=3.12"
readme = "README.md"
license = { text = "MIT" }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/quotes"]
